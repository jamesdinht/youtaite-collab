sudo: required
dist: trusty

addons:
  chrome: stable
  sonarcloud:
    organization: "youtaite-collab"

matrix:
  include:
    - language: csharp
      mono: none
      dotnet: 2.0.0
      before_script:
        - bash sonar-install.sh
      install:
        - dotnet restore
      script:
        - bash sonar-build.sh
        - dotnet test Collab.API.Tests

    - language: node_js
      node_js:
        - "8.9.4"
      cache:
        directories:
          - "node_modules"
      before_script:
        - cd Collab.Interface
        - npm install -g @angular/cli
        - npm install
      script:
        - ng build
        - ng test